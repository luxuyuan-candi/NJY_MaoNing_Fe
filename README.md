# NJY_MaoNing_Fe

猫宁平台独立微信小程序前端。

默认接口域名已指向新的独立后端前缀 `https://www.njwjxy.cn:30443/maoning`，静态资源域名使用 `maoning` namespace 下 MinIO 的代理访问路径 `https://www.njwjxy.cn:30443/maoning/api/assets/public`，配置位于 `utils/config.js`。
