<view class="page-shell home-page">
  <view class="home-stage">
    <image class="home-reference" src="../../images/home-reference.jpg" mode="aspectFill"></image>

    <view class="video-shell">
      <video
        class="feature-video"
        src="{{videoList[currentVideoIndex].src}}"
        poster="../../images/home-reference.jpg"
        controls
        show-center-play-btn
        objectFit="contain"
        enable-progress-gesture
        page-gesture
      ></video>
    </view>

    <view class="video-tabs">
      <block wx:for="{{videoList}}" wx:key="id">
        <view
          class="video-tab {{currentVideoIndex === index ? 'video-tab-active' : ''}}"
          bindtap="selectVideo"
          data-index="{{index}}"
        >
          {{item.title}}
        </view>
      </block>
    </view>
  </view>
</view>
